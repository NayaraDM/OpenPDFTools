# OpenPDFTools
aplicação web gratuita e de código aberto para manipulação de arquivos PDF. Permite unir múltiplos documentos, dividir PDFs e extrair páginas específicas diretamente no navegador, garantindo privacidade e rapidez ao processar os arquivos localmente.
